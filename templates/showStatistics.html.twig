{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

        <div class="form-group">
            <h3>Tvoje statistiky</h3>

            <table class="table"  id="table_statistics" style="margin-top: 3em">
                <thead>
                <tr class="row justify-content-md-center">
                    <th class="col-sm ">Úspěšné TH</th>
                    <th class="col-sm ">Neúspěšné TH</th>
                    <th class="col-sm text-center">Dva body</th>
                    <th class="col-sm text-center">Tři body</th>
                    <th class="col-sm text-center">Fauly</th>
                    <th class="col-sm text-right">Body celkem</th>
                    <th class="col-sm"></th>
                </tr>
                </thead>
                <tbody id="table_matches">
                {% for stats in statistics %}
                    <tr class="row">
                        <td class="col-sm text-center">{{ stats.successfulFt }}</td>
                        <td class="col-sm text-center">{{ stats.unsuccessfulFt }}</td>
                        <td class="col-sm text-center">{{ stats.twoPoints}}</td>
                        <td class="col-sm text-center">{{ stats.threePoints }}</td>
                        <td class="col-sm text-center">{{ stats.fouls }}</td>
                        <td class="col-sm text-center">{{ stats.points }}</td>
                        <th class="col-sm text-right">
{#                            <a href="#">Nominace</a>#}
{#                        </th>#}
                        {#                <td class="columns small-3 text-center">#}
                        {#                    <a href="{{ path('module-scenario-edit', {'scenarioId': scenario.id, 'action': 'edit'}) }}">{{ siteMacros.awesomeIcon('edit') }}&nbsp;Editovat#}
                        {#                    </a>&nbsp;<a href="#" onclick="deleteScenario('{{ path('module-scenario-delete', {'scenarioId': scenario.id}) }}')">{{ siteMacros.awesomeIcon('trash') }}&nbsp;Smazat</a>#}
                        {#                    <br>#}
                        {#                    <a href="{{ path('module-scenario-edit-state', {'scenarioId': scenario.id, 'state': 0}) }}">{{ siteMacros.awesomeIcon('ban') }}&nbsp;Nastavit jako neaktivní</a>#}
                        {#                </td>#}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}